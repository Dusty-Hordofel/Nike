import React from "react";

const CheckoutPage = async () => {
  // const save = await saveCart([]);
  // console.log("🚀 ~ CheckoutPage ~ save:", save);

  return <div>CheckoutPage</div>;
};

export default CheckoutPage;
